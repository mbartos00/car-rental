# Base URL of the NestJS API (server-side only, not exposed to the browser)
API_URL=http://localhost:5500/api

# Stripe TEST publishable key (inlined into the client bundle at build time)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_xxx
